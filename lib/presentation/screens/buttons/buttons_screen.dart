import 'package:flutter/material.dart';

class ButtonsSreens extends StatelessWidget {
  const ButtonsSreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Buttons Screen'),
      ),
      body: const Placeholder(),
    );
  }
}
